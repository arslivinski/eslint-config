'use strict';

const ERROR = 'error';

module.exports = {
  parserOptions: {
    sourceType: 'module',
  },
  rules: {
    // Unicorn ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    'unicorn/prefer-module': ERROR,
  },
};
